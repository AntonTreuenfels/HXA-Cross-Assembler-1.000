; Hobby Cross-Assembler Test 010b
; Output Files\Same Directory
; ok: default extensions

; by Anton Treuenfels

; first created: 01/05/22
; last revised:  04/03/23

; no errors to detect

        // %testdir% is the directory holding all HXA tests

        .listfile   "test010b_sd"    ; should create %testdir%test010b_sd.lst
        .errfile    "test010b_sd"    ; should create %testdir%test010b_sd.err (if any errors)

        .objfile    "test010b_sd"    ; should create %testdir%test010b_sd.obj

; -------------------------------

        .cpu    "T_16_L"             ; required psop
        .org    $1000                ; required psop

; -------------------------------

        .byte   $00                  ; not much too it !

; -------------------------------

        .end
